# 🔐 微信登录签名配置指南

## 🎉 好消息

微信客户端已经成功启动了！这说明：
- ✅ `<queries>` 配置修复成功
- ✅ 微信SDK集成正确
- ✅ APPID配置正确

## ❌ 当前问题

**错误提示**: "签名不对，请检查签名是否与微信开放平台上的签名一致"

**原因**: 微信开放平台配置的应用签名与当前APK的实际签名不匹配

---

## 📋 解决方案

### 方案1: 获取当前签名并在微信开放平台配置（推荐用于测试）

#### Step 1: 获取应用签名

**方法A: 使用微信官方签名工具（最准确）**

1. 下载微信官方签名生成工具：
   - 地址：https://developers.weixin.qq.com/doc/oplatform/Downloads/Android_Resource.html
   - 工具名称：`gen_signature_android.apk` 或 `签名生成工具`

2. 安装签名工具到手机：
   ```bash
   .\adb install gen_signature_android.apk
   ```

3. 在手机上打开签名工具
4. 输入应用包名：`net.qsgl365`
5. 点击"获取签名"
6. 工具会显示签名的MD5值（去掉冒号，全部小写）

**预期输出示例**：
```
91adf554...（32位16进制字符串）
```

---

**方法B: 使用命令行获取（需要转换）**

当前Debug证书的SHA1签名：
```
SHA1: 80:00:A6:A6:62:BF:4E:20:22:00:38:59:74:83:B8:1C:E5:EF:0C:BE
```

但微信需要的是**应用签名的MD5格式**，而不是证书的SHA1。

**正确获取方式**：

1. 从已安装的APK获取签名：
   ```bash
   .\adb pull /data/app/~~XXX/net.qsgl365-XXX/base.apk temp.apk
   keytool -printcert -jarfile temp.apk
   ```

2. 或使用专门的工具：
   ```bash
   # 需要先安装jarsigner
   jarsigner -verify -verbose -certs app\build\outputs\apk\debug\app-debug.apk
   ```

---

**方法C: 使用在线工具**

如果手头没有工具，可以：
1. 将 `app\build\outputs\apk\debug\app-debug.apk` 上传到电脑
2. 使用在线APK分析工具获取签名
3. 或使用Android Studio的 `Build > Generate Signed Bundle/APK` 查看

---

#### Step 2: 在微信开放平台配置签名

1. 访问：https://open.weixin.qq.com/
2. 登录后进入"管理中心"
3. 找到应用"365酒水"（APPID: wx19d89333ff0d3efe）
4. 点击"修改"或"配置"
5. 找到"应用签名"字段
6. 填入从Step 1获取的签名（**32位16进制字符串，去掉冒号，全部小写**）

**示例**：
```
原始签名: 80:00:A6:A6:62:BF:4E:20:22:00:38:59:74:83:B8:1C:E5:EF:0C:BE
配置填写: 8000a6a662bf4e20220038597483b81ce5ef0cbe
```

7. 保存配置
8. **等待5-10分钟**让配置生效
9. 重新测试微信登录

---

### 方案2: 使用已配置的签名重新打包（用于生产环境）

如果微信开放平台已经配置了某个签名，需要使用对应的keystore重新签名APK。

#### Step 1: 检查项目中的签名文件

项目中有：
```
app/my-release-key.jks
```

这可能是已经配置好的Release签名。

#### Step 2: 获取该签名的MD5

```bash
keytool -list -v -keystore app\my-release-key.jks
```

输入密码后查看MD5指纹。

#### Step 3: 使用该签名打包

修改 `app/build.gradle`：

```gradle
android {
    ...
    
    signingConfigs {
        debug {
            storeFile file("my-release-key.jks")
            storePassword "你的密码"
            keyAlias "你的别名"
            keyPassword "你的密钥密码"
        }
    }
    
    buildTypes {
        debug {
            signingConfig signingConfigs.debug
        }
    }
}
```

然后重新编译：
```bash
.\gradlew.bat clean assembleDebug
.\adb install -r app\build\outputs\apk\debug\app-debug.apk
```

---

## 🎯 快速测试方法（临时方案）

如果只是为了测试功能，最简单的方法：

### 使用微信官方签名工具

1. **下载工具**：
   ```bash
   # 访问微信开放平台下载页
   # https://developers.weixin.qq.com/doc/oplatform/Downloads/Android_Resource.html
   ```

2. **安装到手机**：
   ```bash
   .\adb install 微信签名生成工具.apk
   ```

3. **获取签名**：
   - 打开签名工具
   - 输入：`net.qsgl365`
   - 点击"获取签名"
   - 复制显示的签名

4. **配置到微信开放平台**：
   - 登录 https://open.weixin.qq.com/
   - 找到365酒水应用
   - 填入刚才获取的签名
   - 保存

5. **等待5分钟后重试**

---

## 📊 签名对照表

| 证书类型 | 用途 | 签名来源 |
|---------|------|---------|
| Debug证书 | 开发测试 | `~/.android/debug.keystore` |
| Release证书 | 生产发布 | `app/my-release-key.jks` |

| 微信要求 | 格式 | 示例 |
|---------|------|------|
| 应用签名 | 32位MD5 | `8000a6a662bf4e20220038...` |
| 去掉冒号 | 小写 | **不是** `80:00:A6:A6:...` |

---

## 🔍 诊断当前签名

### 当前Debug证书信息
```
证书文件: C:\Users\Administrator\.android\debug.keystore
SHA1: 80:00:A6:A6:62:BF:4E:20:22:00:38:59:74:83:B8:1C:E5:EF:0C:BE
创建日期: 2025年11月27日
```

### 需要的信息
- ❓ 微信开放平台当前配置的签名是什么？
- ❓ 是否需要使用特定的签名？
- ❓ 是测试环境还是生产环境？

---

## 💡 建议操作步骤

### 如果是测试环境（推荐）

1. **下载微信签名生成工具**
   - 地址：https://developers.weixin.qq.com/doc/oplatform/Downloads/Android_Resource.html
   - 下载 `gen_signature_android.apk`

2. **安装并获取签名**
   ```bash
   .\adb install gen_signature_android.apk
   ```
   - 在手机上打开工具
   - 输入 `net.qsgl365`
   - 获取签名

3. **配置到微信开放平台**
   - 访问：https://open.weixin.qq.com/
   - 修改应用签名
   - 保存并等待生效

4. **重新测试**
   - 等待5-10分钟
   - 重新打开APP
   - 点击微信登录

### 如果是生产环境

1. **确认Release签名**
   ```bash
   keytool -list -v -keystore app\my-release-key.jks
   ```

2. **使用Release签名打包**
   ```bash
   .\gradlew.bat assembleRelease
   ```

3. **安装Release版本**
   ```bash
   .\adb install -r app\build\outputs\apk\release\app-release.apk
   ```

---

## 📞 需要的信息

请提供以下信息以便进一步协助：

1. **微信开放平台是否有访问权限？**
   - [ ] 是，可以登录并修改配置
   - [ ] 否，需要申请权限

2. **当前环境**
   - [ ] 测试环境（可以修改签名）
   - [ ] 生产环境（签名已固定）

3. **my-release-key.jks的密码**
   - 如果知道密码，可以使用它来打包

---

## 🚀 立即行动

**推荐方案**（最简单）：

1. 📥 下载微信签名生成工具APK
2. 📱 安装到测试手机
3. 🔑 获取当前应用的签名
4. ⚙️ 在微信开放平台配置该签名
5. ⏰ 等待5分钟
6. ✅ 重新测试微信登录

---

**微信开放平台地址**: https://open.weixin.qq.com/  
**应用APPID**: wx19d89333ff0d3efe  
**应用包名**: net.qsgl365  
**签名工具下载**: https://developers.weixin.qq.com/doc/oplatform/Downloads/Android_Resource.html

---

**当前状态**: ✅ 功能代码完成，⚠️ 需要配置签名  
**下一步**: 获取应用签名并在微信开放平台配置
