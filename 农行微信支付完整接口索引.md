# 农行综合收银台 V4.0 微信支付接口完整索引

## 📁 目录结构分析

```
K:\365-android\综合收银台接口包_V4.0软件包\
│
├── demo/                              # 核心演示代码
│   ├── 【微信支付相关】
│   │   ├── MerchantPaymentUnified.aspx        ⭐⭐⭐⭐⭐ 【最重要】统一支付接口
│   │   ├── GetWeiXinAuthInfo.aspx              ⭐⭐⭐    微信人脸认证
│   │   ├── MerchantPayCancel.aspx              ⭐⭐⭐    支付撤销/退款
│   │   ├── MerchantRealTimeRegister.aspx       ⭐⭐     实时结算注册
│   │   ├── DownloadStatement.aspx              ⭐⭐     对账单下载
│   │   ├── MerchantQueryOrder.aspx             ⭐⭐     订单查询
│   │   └── Merchant.html                       ⭐       菜单导航
│   │
│   ├── 【其他支付方式】
│   │   ├── AlipayRequest.aspx                          支付宝支付
│   │   ├── ABCQRPayReq.aspx                            农行二维码支付
│   │   ├── EWalletPayReq.aspx                          电子钱包支付
│   │   └── [其他...]
│   │
│   ├── 【委托代扣相关】
│   │   ├── EntrustPayment.aspx
│   │   ├── EntrustSignForBank.aspx
│   │   └── EntrustQuerySign.aspx
│   │
│   ├── 【商户管理相关】
│   │   ├── MerchantBatchRegister.aspx
│   │   ├── RegSubMerchantInfo.aspx
│   │   └── UpdateSubMerchantStatus.aspx
│   │
│   ├── 【代理相关】
│   │   ├── AgentPayment.aspx
│   │   ├── AgentBatchPayment.aspx
│   │   └── [其他...]
│   │
│   ├── CBP/                                 # 清算汇总平台
│   │   ├── Test_3Pay.aspx                       三方支付测试
│   │   ├── Test_3PayNotify.aspx                 三方支付回调测试
│   │   ├── Search_3Pay.aspx                     三方支付查询
│   │   └── SignTools.aspx                       签名工具
│   │
│   ├── EBPP/                                # 电子账单支付平台
│   │   └── [EBPP相关文件...]
│   │
│   ├── Bin/                                 # 编译后的程序集
│   │   └── [依赖库文件...]
│   │
│   ├── Web.config                           # 配置文件
│   └── MerchantQuickPayment.html             # 快速支付示例页面
│
├── cert/                                    # 证书文件
│   └── [签名证书...]
│
└── Readme.txt                               # 说明文档
```

---

## 🎯 微信支付文件详细说明

### 1. **MerchantPaymentUnified.aspx** ⭐⭐⭐⭐⭐ 【必看】

**完整路径**：`demo/MerchantPaymentUnified.aspx`

**功能**：统一支付接口（支持微信、支付宝等多种支付方式）

**关键类**：
```csharp
com.abc.trustpay.client.ebus.UnifiedPaymentRequest
```

**核心参数配置**：
```csharp
var request = new UnifiedPaymentRequest();

// 1. 订单信息（dicOrder）
request.dicOrder["TerminalNo"] = "终端号";
request.dicOrder["OrderNo"] = "订单号";
request.dicOrder["OrderAmount"] = "100.00";           // 订单金额
request.dicOrder["OrderDate"] = "2026/01/07";
request.dicOrder["OrderTime"] = "12:00:00";

// 2. 微信支付特有参数
request.dicOrder["PayTypeID"] = "小程序支付";          // 支付类型
request.dicOrder["AccountNo"] = "wxxxxxxxxxxx";        // 小程序APPID
request.dicOrder["OpenID"] = "oxxxxxxxxxx";            // 用户OpenID

// 3. 支付请求信息（dicRequest）
request.dicRequest["PaymentType"] = "8";               // 8=微信支付
request.dicRequest["PaymentLinkType"] = "WAP";
request.dicRequest["NotifyType"] = "Email";

// 4. 发起请求
request.postRequest();
```

**关键字段说明**：

| 字段 | 值 | 说明 |
|------|-----|------|
| `PaymentType` | `8` | **必须=8 表示微信支付** |
| `PayTypeID` | 支付类型 | 公众号/小程序/APP/刷卡支付 |
| `AccountNo` | APPID | 小程序APPID（PublicPlatform或App）或授权码 |
| `OpenID` | 用户ID | 用户在该商户AppID下的OpenID |
| `OrderAmount` | 金额 | 支付金额，单位：元 |
| `OrderDate` | 日期 | 格式：YYYY/MM/DD |
| `OrderTime` | 时间 | 格式：HH:MM:SS |

---

### 2. **GetWeiXinAuthInfo.aspx** ⭐⭐⭐

**完整路径**：`demo/GetWeiXinAuthInfo.aspx`

**功能**：获取微信人脸认证信息（用于人脸支付）

**关键类**：
```csharp
com.abc.trustpay.client.ebus.GetWeiXinAuthInfo
```

**调用方法**：
```csharp
var authInfo = new GetWeiXinAuthInfo();
authInfo.merchantInfoRequest["StoreId"] = "门店ID";
authInfo.merchantInfoRequest["StoreName"] = "门店名称";
authInfo.merchantInfoRequest["DeviceId"] = "设备ID";
authInfo.merchantInfoRequest["SubAppId"] = "wxxxxxxxxxxx";  // 子商户APPID
authInfo.merchantInfoRequest["RawData"] = "原始数据";
authInfo.postJSONRequest();

// 获取响应
string AuthInfo = JSON.GetKeyValue("AuthInfo");
string ExpiresIn = JSON.GetKeyValue("ExpiresIn");
```

**响应字段**：
- `ReturnCode`：返回码（0000=成功）
- `ErrorMessage`：错误消息
- `AuthInfo`：认证信息（用于调用微信人脸SDK）
- `ExpiresIn`：过期时间

---

### 3. **MerchantPayCancel.aspx** ⭐⭐⭐

**完整路径**：`demo/MerchantPayCancel.aspx`

**功能**：支付撤销和退款处理

**调用方法**：
```csharp
var cancel = new MerchantPayCancel();
cancel.dicRequest["OrigOrderNo"] = "20260107001";       // 原订单号
cancel.dicRequest["MerchantFlag"] = "W";                // W=微信 Z=支付宝
cancel.dicRequest["CancelAmount"] = "100.00";           // 退款金额
cancel.dicRequest["CancelReason"] = "用户申请";         // 退款原因
cancel.postRequest();
```

**关键参数**：
| 参数 | 值 | 说明 |
|------|-----|------|
| `MerchantFlag` | `W` | 微信支付 |
| `MerchantFlag` | `Z` | 支付宝支付 |
| `OrigOrderNo` | 订单号 | 需要撤销的原订单号 |
| `CancelAmount` | 金额 | 退款金额，单位：元 |

---

### 4. **MerchantRealTimeRegister.aspx** ⭐⭐

**完整路径**：`demo/MerchantRealTimeRegister.aspx`

**功能**：实时结算注册（用于记录交易信息）

**支付方式字段**：
```
TrnxCode:
  8 = 微信支付
  9 = 支付宝
```

**交易状态字段**（对应微信对账单）：
```
TrnxType:
  SUCCESS = 支付成功
  REFUND = 已退款
  CLOSED = 未支付已关闭
```

**用户识别字段**（对应微信对账单）：
```
CustomId:
  对应微信对账单中的"用户标识"
  示例：otDNot6uuTOGLptNsyEB97uzRUQI
```

---

### 5. **DownloadStatement.aspx** ⭐⭐

**完整路径**：`demo/DownloadStatement.aspx`

**功能**：对账单下载

**支持的对账单类型**：
```
WXALIPAY  =  微信支付宝对账单
W         =  微信对账单
Z         =  支付宝对账单
```

**使用示例**：
```csharp
// 查询对账单
var query = new DownloadStatement();
query.dicRequest["StatementType"] = "W";  // 微信
query.dicRequest["QueryDate"] = "2026-01-07";
query.postRequest();
```

---

### 6. **Merchant.html** ⭐

**完整路径**：`demo/Merchant.html`

**功能**：商户操作菜单主页

**包含的微信支付链接**：
```html
<a href='GetWeiXinAuthInfo.aspx'>获取微信人脸验证</a>
```

---

## 🔗 关键类库依赖

### 核心命名空间
```csharp
using com.abc.trustpay.client;              // 基础客户端
using com.abc.trustpay.client.ebus;         // 支付接口
using com.abc.util;                         // 工具类（签名等）
```

### 关键类型
```csharp
// 支付请求
UnifiedPaymentRequest          // 统一支付请求
MerchantPayCancel              // 支付撤销
GetWeiXinAuthInfo              // 微信认证

// 查询和管理
MerchantQueryOrder             // 订单查询
DownloadStatement              // 对账单

// 工具
SignTools                      // 签名验证
XMLDocument                    // XML处理
MerchantConfig                 // 商户配置
JSON                          // JSON处理
```

---

## 📊 微信支付类型代码速查

| 代码 | 支付方式 | 需要OpenID | 需要APPID | 说明 |
|------|---------|-----------|----------|------|
| 8 | 微信支付（通用） | - | ✓ | 微信支付总入口 |
| - | 公众号支付 | ✓ | ✓ | 在公众号内支付 |
| - | 小程序支付 | ✓ | ✓ | 在小程序内支付 |
| - | APP支付 | - | ✓ | 第三方APP支付 |
| - | 刷卡支付 | - | - | 需要扫码授权码 |

---

## 🔐 安全相关

### 签名工具
**位置**：`demo/CBP/SignTools.aspx`

**功能**：用于对请求和响应进行数字签名

```csharp
// 签名
string signature = com.abc.util.SignTools.Sign(requestString);

// 验签
bool isValid = com.abc.util.SignTools.Verify(responseString);

// 解签
string unsignedContent = com.abc.util.SignTools.UnSign(signedString);
```

### 证书管理
**位置**：`cert/` 目录

**内容**：商户签名证书和银行公钥证书

---

## 🔄 完整支付流程

```
1. 用户在应用中选择微信支付
   ↓
2. 调用 MerchantPaymentUnified
   - PaymentType = "8"
   - PayTypeID = 支付类型（公众号/小程序等）
   - AccountNo = 微信APPID
   - OpenID = 用户OpenID（如需要）
   ↓
3. 农行返回支付参数或支付链接
   ↓
4. 前端调用微信支付
   - 获取prepay_id
   - 唤起微信支付
   ↓
5. 用户在微信客户端完成支付
   ↓
6. 微信通知农行支付结果
   ↓
7. 农行异步回调商户服务器
   ↓
8. 商户验证签名后更新订单状态
   ↓
9. 如需查询订单状态
   - 调用 MerchantQueryOrder
   ↓
10. 如需退款
    - 调用 MerchantPayCancel
    - MerchantFlag = "W"
```

---

## 📝 示例代码合集

### 完整的微信支付请求
```csharp
protected void OnWeChatPayment(object sender, EventArgs e)
{
    try
    {
        // 1. 创建支付请求
        var request = new UnifiedPaymentRequest();
        
        // 2. 设置基本订单信息
        request.dicOrder["TerminalNo"] = "123456";
        request.dicOrder["OrderNo"] = "ORD" + DateTime.Now.Ticks;
        request.dicOrder["OrderAmount"] = txtAmount.Text;
        request.dicOrder["OrderDate"] = DateTime.Now.ToString("yyyy/MM/dd");
        request.dicOrder["OrderTime"] = DateTime.Now.ToString("HH:mm:ss");
        
        // 3. 设置微信支付参数
        request.dicOrder["PayTypeID"] = ddlPayType.SelectedValue;  // 公众号/小程序/APP
        request.dicOrder["AccountNo"] = txtAppId.Text;              // 小程序APPID
        request.dicOrder["OpenID"] = txtOpenId.Text;                // 用户OpenID
        
        // 4. 设置支付请求参数
        request.dicRequest["PaymentType"] = "8";                    // 8=微信支付
        request.dicRequest["PaymentLinkType"] = "WAP";
        
        // 5. 提交请求
        request.postRequest();
        
        // 6. 处理响应
        if (request.isSuccess)
        {
            // 支付成功，获取支付参数或链接
            string paymentLink = request.dicResponse["PaymentLink"];
            // 前端跳转到支付链接或唤起支付
        }
        else
        {
            // 支付失败
            lblMessage.Text = "支付请求失败: " + request.errorMsg;
        }
    }
    catch (Exception ex)
    {
        lblMessage.Text = "错误: " + ex.Message;
    }
}
```

### 处理支付回调
```csharp
protected void Page_Load(object sender, EventArgs e)
{
    if (!IsPostBack)
    {
        try
        {
            // 1. 获取农行回调的签名消息
            string msg = Request["MSG"];
            
            // 2. 解签
            string unsignedContent = com.abc.util.SignTools.UnSign(msg);
            
            // 3. 解析XML
            var xmlDoc = new com.abc.trustpay.client.XMLDocument(unsignedContent);
            
            // 4. 获取支付结果
            string status = xmlDoc.getValue("Status");           // 1=成功
            string orderNo = xmlDoc.getValue("CBPOrderNo");      // 订单号
            string merchantId = xmlDoc.getValue("MerchantID");   // 商户ID
            
            // 5. 更新数据库订单状态
            if (status == "1")
            {
                // 支付成功
                UpdateOrderStatus(orderNo, "PAID");
            }
            else
            {
                // 支付失败
                UpdateOrderStatus(orderNo, "FAILED");
            }
        }
        catch (Exception ex)
        {
            // 日志记录
            Log.Error("支付回调处理失败: " + ex.Message);
        }
    }
}
```

---

## ✅ 集成检查清单

- [ ] 获取商户ID和签名证书
- [ ] 配置MerchantConfig（商户信息）
- [ ] 实现 MerchantPaymentUnified 调用
- [ ] 设置 PaymentType = "8"（微信支付）
- [ ] 获取用户OpenID和APPID
- [ ] 实现回调处理（ReciveServerPage.aspx）
- [ ] 实现签名验证（SignTools）
- [ ] 测试支付流程
- [ ] 实现订单查询
- [ ] 实现退款流程
- [ ] 对接前端支付界面
- [ ] 上线前安全审计

---

## 📞 官方文档

**农行综合收银台API文档**：
```
https://bank.u51.com/ebus-two/docs/#/
```

**本地文档**：
```
K:\365-android\综合收银台接口包_V4.0软件包\Readme.txt
```

---

**文档版本**：v2.0  
**更新时间**：2026-01-07  
**包含内容**：
- ✅ 6个微信支付核心文件
- ✅ 完整参数说明和映射
- ✅ 代码示例和片段
- ✅ 支付流程图
- ✅ 集成检查清单
